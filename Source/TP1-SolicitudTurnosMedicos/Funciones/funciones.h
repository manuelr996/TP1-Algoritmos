 void   ProcMedicos(       ); // Descarga archivo Medicos.
 void   ProcEspecialidad(      );  // Descarga archivo Especialidades.
 void   ProcTurnos(       );  // Descarga archivo TurnosDiaHora.
 void   LstTurnos(        ); // Lista ord. x Especialidades, Días y Turnos del vuelco del archivo TurnosDiasHora.
 void   ProcTurnos(       ); // Descarga archivo SolicitudesTurnos y lista líneas de las solicitudes.
 void   LstTurnos(        ); // Lista ord. x Especialidades, Días y Turnos Actualizado con las SolicitudesTurnos.
